@using HairSalon.Models;

@{
  Layout = "_Layout";
}

<h1>Like An Angel Hair Salon Clients</h1>
<ul>

@if (Model.Count == 0)
{
  <p>You have no clients!</p>
}
@if (Model.Count !=0)
{
  @foreach (Client client in Model)
  {
  <li><a href='/clients/@client.GetId()'>@client.GetClientName()</a></li>
  }
}

</ul>
<a href="/clients/new">Add a new client.</a>
<form action="/clients/delete" method="post">
  <button type="submit" name="button">Clear All Clients</button>
</form>
